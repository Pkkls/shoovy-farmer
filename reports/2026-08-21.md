# Shoovy daily — 2026-08-21

One-line verdict: the casino house edge is now measured (~1%, far better than assumed),
but a **new blocker replaced the old one** — Cloudflare is now issuing a JS challenge
("Just a moment") instead of the Railway 429/502 degradation, so today's availability
picture looks different in kind, not just in number.

Sources named inline. Zero new requests to shoovy.wtf today — offline analysis on the
2026-08-20 capture (`data/study/live/2026-08-20/`) plus the Claw sentinel log
(`/root/shoovy/avail.jsonl`, read-only over SSH).

## What changed since yesterday

- **New failure mode**: `avail.jsonl` shows the first Cloudflare 403 "Just a moment" at
  **2026-08-20T20:24:20Z**, replacing the 429/502 Railway pattern reported yesterday. Of
  the last 288 probes (~24h), 75 succeeded (200), 111 got the Cloudflare challenge page,
  the rest timed out. **Uptime ~26%** over the last day — similar magnitude to before, but
  a plain Go/stdlib client cannot pass a JS challenge, so this is a harder wall than a
  rate limiter: no amount of request pacing gets past it. The sentinel itself is alive
  and logging correctly (`ps` confirms `shoovy-sentinel-riscv64` running, one line every
  5 min, no restart storm).
- **Casino EV is now computable** — yesterday's blocker ("odds are behind a JSON
  response, not in static HTML") is resolved: `live/2026-08-20/games_info.json` carries
  the real tables. Computed today (`ctx_execute`, Python, binomial/discrete EV, not
  read raw into context):
  - **cases**: literal `rtp: 99.0` on **all five difficulties** (easy → master) — flat
    1% house edge, no variance in edge across risk level.
  - **keno**: RTP 98.96–99.04% across pick-1 through pick-6 (classic/low/medium/high
    tables) — same ~1% edge band, largely difficulty-invariant.
  - **plinko**: best config (11 rows, high risk) computed EV = **0.9916** (0.84% edge);
    worst (8 rows, medium) = 0.9891 (1.09% edge). Full 5-row range: consistently
    0.98–0.99, never above 1.0.
  - **wheel**: per-bet EV from `layout`/`payouts` (25 segments, 5 payout tiers) —
    betting the 1/3/5 tiers gives EV 0.96 each (4% edge); the 20-tier is worse at 0.84
    (16% edge). Not flat like cases/keno — tier choice matters here.
  - **coinflip** (0.97) and **rock-paper-scissors** (EV 0.96 at p=0.5, unconfirmed house
    RNG symmetry) sit in the same ~1–4% edge band from their literal multipliers.
  - **Still not computable**: mines, dragon_tower (partial: tile/egg counts known,
    landing probability isn't), slots, crash — payout tables exist in the capture but the
    win-probability side is server-computed per round, not literal.

## The number that matters: house edge is ~1%, not the "typical casino" 4-5%

Every game with a literal, computable RTP lands in **99.0–99.16%** (cases, keno, best
plinko config). This is thin enough that a **rakeback rate of ~1% or more would flip
these to breakeven or positive EV**. Rakeback rate itself is still not captured — it's
session-gated (`/api/rakeback`), and there were no valid sessions to query today. This is
the single most actionable unknown left: one authenticated `/api/rakeback` read (when a
session exists) resolves the EV verdict for the whole cases/keno/plinko-high family.

## Rank-1 target

Unchanged from 2026-08-20 (`live/2026-08-20/leaderboard.json`, no fresher capture today):
**Mackie33 at 886,890**. No live leaderboard read today since the site never returned a
clean 200 during today's window.

## Farming gate

`shoovy-farmer/accounts.json` still absent (confirmed today, `find` over the repo).
Blocked pending fresh Kick + shoovy cookies from the operator — same blocker as every
prior day, restated because the rule requires re-checking, not because anything changed.

## Next step (no human needed)

Re-run the EV computation the next time a fresh `games_info.json` lands, to confirm the
tables are stable day-to-day (single-sample right now). Watch whether the Cloudflare
challenge is permanent or intermittent — if 200s keep appearing alongside 403s (as they
did today, 75/288), it's likely a per-request bot score, not a hard block, meaning the
sentinel's plain client will keep sampling successfully even though a scripted farmer
would fail more often.
