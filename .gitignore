# Your credentials — NEVER commit these
accounts.json

# Runtime data (logs, state, stop flag, bot status)
data/*

# ...except the market study: its scripts, request log and raw captures are
# meant to be public and reviewable.
!data/study/

# Compiled binaries (rebuild with build.bat)
bin/
*.exe

*.log

# Python bytecode
__pycache__/

# The chat capture grows without bound; keep the tooling, not the tape.
data/study/chat.jsonl
